{
  "name": "sudoku-api-lambda",
  "version": "1.0.0",
  "description": "Lambda function for serving random Sudoku puzzles",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp -r puzzles dist/",
    "package": "pnpm build && cd dist && zip -r ../sudoku-api.zip .",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.9.3",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@8.15.1"
}

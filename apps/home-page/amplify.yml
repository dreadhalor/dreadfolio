version: 1
applications:
  - appRoot: apps/home-page
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@8.15.1
            - pnpm install
        build:
          commands:
            - pnpm build --filter=home-page
      artifacts:
        baseDirectory: apps/home-page/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*

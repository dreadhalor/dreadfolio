version: 1
applications:
  - appRoot: apps/quipster
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@8.15.1
            - pnpm install
        build:
          commands:
            - pnpm build --filter=quipster
      artifacts:
        baseDirectory: apps/quipster/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*

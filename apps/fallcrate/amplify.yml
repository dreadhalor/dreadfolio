version: 1
applications:
  - appRoot: apps/fallcrate
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@8.15.1
            - pnpm install
        build:
          commands:
            - pnpm build --filter=fallcrate
      artifacts:
        baseDirectory: apps/fallcrate/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*

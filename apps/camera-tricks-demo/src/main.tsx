import React from 'react';
import ReactDOM from 'react-dom/client';
import RoomGallery from './demos/room-gallery';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RoomGallery />
  </React.StrictMode>
);

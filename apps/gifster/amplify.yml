version: 1
applications:
  - appRoot: apps/gifster
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@8.15.1
            - pnpm install
        build:
          commands:
            - pnpm build --filter=gifster
      artifacts:
        baseDirectory: apps/gifster/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*

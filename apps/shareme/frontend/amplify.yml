version: 1
applications:
  - appRoot: apps/shareme/frontend
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../../..
            - npm install -g pnpm@8.15.1
            - pnpm install
        build:
          commands:
            - pnpm build --filter=shareme-frontend
      artifacts:
        baseDirectory: apps/shareme/frontend/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*

version: 1
applications:
  - appRoot: apps/pathfinder-visualizer
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@8.15.1
            - pnpm install
        build:
          commands:
            - pnpm build --filter=pathfinder-visualizer
      artifacts:
        baseDirectory: apps/pathfinder-visualizer/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*

version: 1
applications:
  - appRoot: apps/resume
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@8.15.1
            - pnpm install
        build:
          commands:
            - pnpm build --filter=resume
      artifacts:
        baseDirectory: apps/resume/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
